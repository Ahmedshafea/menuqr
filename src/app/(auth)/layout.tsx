import Link from "next/link"; import { QrCode } from "lucide-react"; import { getTranslations } from "next-intl/server"; import { LanguageSwitcher } from "@/components/language-switcher";
export default async function AuthLayout({children}:{children:React.ReactNode}){const t=await getTranslations("auth");return <main className="auth-shell"><section className="auth-side"><Link href="/" className="brand"><span><QrCode/></span>MenuQR</Link><LanguageSwitcher/><div><p>{t("sideLabel")}</p><h2>{t("sideTitle")}</h2></div></section><section className="auth-main">{children}</section></main>}
